<UserControl x:Class="battleship_client.RoomsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" MinHeight="320" MinWidth="355">
    <Grid Name="grid"  Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition/>
            <RowDefinition Height="60"/>
        </Grid.RowDefinitions>
        <Label FontSize="20" Content="Available games" HorizontalAlignment="Left" Margin="10,10,0,0" VerticalAlignment="Top" Height="40" Width="160" RenderTransformOrigin="-2.328,-1.071"/>
        <Button Name="createButton" FontSize="20" Content="Create new game" HorizontalAlignment="Stretch" Margin="175,10,10,0" VerticalAlignment="Top" Height="40" Click="createButton_Click"/>
        <DataGrid Name="roomsGrid" ItemsSource="{Binding}" AutoGenerateColumns="False" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="10" Grid.Row="1" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Player" Binding="{Binding Name}" CanUserReorder="True" CanUserResize="True" 
                                            CanUserSort="True" Width="*" IsReadOnly="True"/>
                <DataGridTextColumn Header="Creation time" Binding="{Binding CreationTime}" CanUserReorder="True" CanUserResize="True" 
                                            CanUserSort="True" Width="*" IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>
        <Button Name="joinButton" FontSize="20" Content="Join" HorizontalAlignment="Stretch" Margin="175,10,10,0" VerticalAlignment="Top" Height="40" Grid.Row="2" Click="joinButton_Click"/>
    </Grid>
</UserControl>
