<UserControl x:Class="battleship_client.WaitingOpponentPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
             MinHeight="160" MinWidth="300">


    <Grid Background="#88FFFFFF">
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Label Content="Wait you opponent..." HorizontalAlignment="Stretch" Margin="10" VerticalAlignment="Stretch"/>
        <Button Name="backButton" Content="Back" HorizontalAlignment="Stretch" Margin="10" Grid.Row="1" VerticalAlignment="Stretch" Width="280" Height="80" Click="backButton_Click"/>
    </Grid>

</UserControl>
